package main

/*
import (
	"fmt"
)

func main() {

	var myarray = []string{}

	myarray = append(myarray, "Samuel", "Kiran")
	fmt.Print(myarray)

	mymap := make(map[string]string)

	mymap["firstname"] = "samuel"
	mymap["lastname"] = "Kiran"
	mymap["email"] = "slamu@gmail.com"
	var searchvalue = "slamu@gmail.com"
	for i, j := range mymap {
		if j == searchvalue {
			fmt.Println("found Key : ", i)

		}
	}
	fmt.Print(mymap["firstname\n"])

	var slice = []string{}

	slice = append(slice, "appending", "elements", "to", "an", "infinite", "or", "shrinked size", "array\n")
	fmt.Print(slice)

	var nums [10]int
	for i := 0; i < 10; i++ {
		nums[i] = i
	}
	fmt.Println(nums)

	var my_slice = []int{}
	my_slice = append(my_slice, 30)
	my_slice = append(my_slice, 70)
	my_slice = append(my_slice, 100)
	fmt.Print(my_slice)

	slice_two := []string{}
	slice_two = append(slice_two, "hello", "how are you", "hope you are fine\n")
	fmt.Print(slice_two)

	var goodmap = make(map[string][]string)
	goodmap["Good"] = append(goodmap["Good"], "Morning")
	goodmap["Good"] = append(goodmap["Good"], "Evening")
	goodmap["Good"] = append(goodmap["Good"], "Night")
	fmt.Print(goodmap)

	// creating structures

	type userdata struct {
		firstName string
		lastName  string
		email     string
		mobile    int
		age       int
	}

	var userdetails = []userdata{
		{firstName: "samuel",
			lastName: "Lamu",
			email:    "abc@gmail.com",
			mobile:   1234567,
			age:      23,
		},
		{
			firstName: "sonu",
			lastName:  "lol",
			email:     "cba@gmail.com",
			mobile:    987643,
			age:       77,
		},
	}
	fmt.Print(userdetails)
}
*/
